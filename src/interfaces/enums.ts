export enum Role {
    ADMIN = 'admin',
    USER = 'user',
}

export enum PostType {
    ESTATE = 'estate',
    NEWS = 'news',
}

export enum Environment {
    DEVELOPMENT = 'development',
    PRODUCTION = 'production',
}
