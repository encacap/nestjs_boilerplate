export enum Role {
    ADMIN = 'admin',
    USER = 'user',
}

export enum ImageFolder {
    ESTATE = 'estate',
    NEWS = 'news',
    OTHER = 'other',
}

export enum Environment {
    DEVELOPMENT = 'development',
    PRODUCTION = 'production',
}
